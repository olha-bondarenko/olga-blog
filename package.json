{
    "name": "olga-blog",
    "version": "1.0.0",
    "engines" : { 
        "node" : ">=16.0.0"
    },
    "scripts": {
        "start": "cd backend/built && node src/server.js",
        "prebuild": "cd backend && npm run build",
        "build": "cd frontend && npm run build"
    }
}